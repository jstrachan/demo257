apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/jstrachan/demo257:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/jstrachan/demo257:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: demo257
        chartPath: charts/demo257
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/jstrachan/demo257'
          image.tag: '{{.DIGEST_HEX}}'
